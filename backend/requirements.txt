# Core
fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings
python-dotenv
httpx

# AI & Agents
langchain
langchain-google-genai
langchain-community
langgraph
langsmith
huggingface_hub
# NOTE: sentence-transformers and faiss-cpu removed - they were unused
# (the SemanticChunker is pure regex/string logic, no embeddings) and
# pulled in torch + NVIDIA CUDA libs, bloating the image to ~3GB and
# overflowing the node disk. Removing them cuts the image dramatically.

# Document Parsing
PyMuPDF
python-docx

# Output
weasyprint

# Auth & Database
sqlalchemy
alembic
psycopg2-binary
python-jose[cryptography]
passlib[bcrypt]
email-validator
# Google OAuth uses httpx directly (already a dependency above) - no
# separate OAuth library needed.

# Testing
pytest
pytest-asyncio

# Dev tools
ruff
black
mypy


